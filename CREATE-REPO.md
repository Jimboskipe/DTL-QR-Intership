# Create GitHub repo + Pages (DTL QR Intern)

1. Public repo e.g. `dtl-qr-internship-portfolio`
2. Upload contents of `portfolio/` to repo root
3. Pages → main / root
4. Paste URL into `COVER.txt` and the DTL apply form
5. Record video; embed Unlisted link in `index.html` when ready
6. Resume filename stays ASCII English only (already set)

No secrets or live account statements.
